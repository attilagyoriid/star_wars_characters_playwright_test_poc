pipeline {
    agent {
        docker {
        image 'playwright/chromium:playwright-1.51.0'
            args """
                --ipc=host 
                -e CI=true
                -v /var/run/docker.sock:/var/run/docker.sock
            """
            }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}

